export class Serie{
    constructor(public id: number, public name: string, public canal: string, public temporadas: number, public descripcion: string, public enlace: string, public imagen: string){
    }
}